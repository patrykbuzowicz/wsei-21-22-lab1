@model IEnumerable<Wsei.Lab1.Services.EndpointStats>

<h2>Endpoint metrics</h2>

<table class="table">
    <thead>
        <tr>
            <th>HTTP method</th>
            <th>Request path</th>
            <th>Request count</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var stats in Model)
        {
            <tr>
                <td>@stats.HttpMethod</td>
                <td>@stats.RequestUrl</td>
                <td>@stats.RequestsCount</td>
            </tr>
        }
    </tbody>
</table>